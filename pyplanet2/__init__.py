"""Blog aggregator: combines RSS/Atom feeds into an Atom feed and a static HTML view."""
__version__ = "0.1.0"
# Only __version__ and the docstring (as description) are read from
# this module; all other metadata lives in pyproject.toml.